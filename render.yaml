services:
  - type: cron
    name: trend-decision-checker
    env: python
    plan: free
    schedule: "0 * * * *"  # Runs every hour at HH:00
    buildCommand: "pip install -r requirements.txt"
    startCommand: "python test_trend_decision.py"
